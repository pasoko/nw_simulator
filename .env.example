# Docker settings
COMPOSE_PROJECT_NAME=ospf-simulator
DOCKER_REGISTRY=docker.io
IMAGE_TAG=latest

# Application settings
APP_PORT=8080
NGINX_WORKER_PROCESSES=auto
NGINX_WORKER_CONNECTIONS=1024

# Development settings
RUST_LOG=info
NODE_ENV=production