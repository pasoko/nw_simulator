# Contributing to OSPF Network Simulator

OSPFネットワークシミュレーターへの貢献をありがとうございます！このドキュメントでは、プロジェクトへの貢献方法について説明します。

## 開発方針

### AIの活用について

このプロジェクトでは、開発効率と品質向上のため、生成AI（主にClaude）を積極的に活用しています：

- **コード生成**: 新機能の実装やボイラープレートコードの生成
- **リファクタリング**: 既存コードの改善と最適化
- **ドキュメント作成**: README、コメント、技術文書の作成
- **バグ修正**: エラーの特定と修正案の提案
- **テストコード**: ユニットテストとインテグレーションテストの作成

貢献者の方々も、AIツールの使用を推奨します。ただし、以下の点にご注意ください：

1. **コードレビュー**: AIが生成したコードは必ず人間がレビューすること
2. **セキュリティ**: 機密情報やプライベートなコードをAIに入力しないこと
3. **ライセンス**: 生成されたコードがプロジェクトのライセンスと互換性があることを確認すること
4. **品質管理**: AIの出力を鵜呑みにせず、プロジェクトの品質基準に合致することを確認すること

## 貢献の方法

### 1. Issue の作成

バグ報告や機能提案は、GitHubのIssueとして作成してください：

- **バグ報告**: 再現手順、期待される動作、実際の動作を明記
- **機能提案**: 提案の背景、実装案、期待される効果を説明

### 2. Pull Request の作成

1. リポジトリをフォーク
2. 機能ブランチを作成 (`git checkout -b feature/amazing-feature`)
3. 変更をコミット (`git commit -m 'feat: Add amazing feature'`)
4. ブランチをプッシュ (`git push origin feature/amazing-feature`)
5. Pull Requestを作成

### 3. コミットメッセージ

以下の形式に従ってください：

```
<type>: <subject>

<body>

<footer>
```

**Type**:
- `feat`: 新機能
- `fix`: バグ修正
- `docs`: ドキュメントのみの変更
- `style`: フォーマット、セミコロンの欠落など
- `refactor`: コードの再構築
- `test`: テストの追加または修正
- `chore`: ビルドプロセスやツールの変更

**例**:
```
feat: ルーター間の帯域幅設定機能を追加

- 各接続に帯域幅を設定可能に
- UIに帯域幅設定フィールドを追加
- ルーティング計算に帯域幅を反映

Closes #123
```

## 開発環境のセットアップ

詳細は[README.md](README.md#開発環境セットアップ)を参照してください。

## コーディング規約

### Rust

- `cargo fmt`でフォーマット
- `cargo clippy`で静的解析
- 安全でないコード（`unsafe`）は避ける
- エラーハンドリングは`Result`型を使用

### JavaScript

- ES6+の機能を積極的に使用
- 関数名は`snake_case`、クラス名は`PascalCase`
- セミコロンは省略しない
- `const`と`let`を使用（`var`は使用しない）

## テスト

すべての新機能にはテストを追加してください：

```bash
# Rustのテスト
cargo test

# JavaScriptのテスト（将来実装予定）
npm test
```

## ドキュメント

- コードには適切なコメントを追加
- 複雑な処理にはドキュメンテーションコメントを記述
- APIの変更はREADMEに反映

## 質問とサポート

- 技術的な質問はGitHub Discussionsで
- バグ報告はGitHub Issuesで
- その他の問い合わせはプロジェクトメンテナーまで

## ライセンス

このプロジェクトへの貢献は、[MITライセンス](LICENSE)の下でライセンスされることに同意したものとみなされます。